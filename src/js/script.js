const menu_open_btn = document.querySelector('.menu');
const menu_close_btn = document.querySelector('.menu-dropdown::after');
const menu_nav = document.querySelectorAll('.menu-dropdown');

// menu_open_btn.addEventListener('click', () => {
// 	menu_nav.forEach(nav_el => { nav_el.classList.add('visible') });
// });

// menu_close_btn.addEventListener('click', () => {
// 	menu_nav.forEach(nav_el => { nav_el.classList.remove('visible') });
// });
